class Solution {
public:
    bool isMatch(string s, string p) {
        int s_len = s.length();
        int p_len = p.length();
        if (!s_len && !p_len) return true;
        if (!s_len || !p_len) return false;

        
    }
};